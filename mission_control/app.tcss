/* Mission Control: flat panes on a dark background, one accent colour. Every pane has a
   title line on top instead of a box; the focused pane's title turns to the accent. */

Screen {
    background: $background;
}

/* ---------------------------------------------------------------- header */

RunHeader {
    height: 3;
    padding: 0 1;
    background: $surface;
    border-bottom: solid $panel;
}

#header-top {
    height: 1;
}

#brand {
    width: auto;
}

#totals {
    width: 1fr;
    text-align: right;
}

#status {
    height: 1;
}

/* ---------------------------------------------------------------- panes */

DataTable, Tree, Detail, #active {
    background: $background;
    border: none;
    border-top: solid $panel;
    border-title-color: $text-muted;
    border-title-style: bold;
    padding: 0 1;
    scrollbar-size-vertical: 1;
    scrollbar-color: $panel;
    scrollbar-background: $background;
}

DataTable:focus, Tree:focus, Detail:focus {
    border-top: solid $accent;
    border-title-color: $accent;
}

DataTable > .datatable--header {
    background: $background;
    color: $text-muted;
    text-style: none;
}

DataTable > .datatable--cursor, Tree > .tree--cursor {
    background: $panel;
    text-style: none;
}

DataTable:focus > .datatable--cursor, Tree:focus > .tree--cursor {
    background: $accent 25%;
}

DataTable > .datatable--hover, Tree > .tree--highlight {
    background: $surface;
}

/* The frame (run list + main area) fills the space between header and footer; inside the
   main area, a screen's own top-level row fills it (not rows inside widgets). */
#frame {
    height: 1fr;
}

#main {
    width: 1fr;
}

#main > Horizontal {
    height: 1fr;
}

#run-list {
    width: 34;
    height: 1fr;
    background: $surface;
    border: none;
    border-top: solid $panel;
    border-title-color: $text-muted;
    border-title-style: bold;
    padding: 0 1;
}

#run-list:focus {
    border-top: solid $accent;
    border-title-color: $accent;
}

#run-list > .option-list--option-highlighted {
    background: $accent 25%;
}

#home {
    width: auto;
    height: auto;
}

/* ---------------------------------------------------------------- footer */

Footer {
    background: $surface;
}

FooterKey {
    background: $surface;
    color: $text-muted;
}

FooterKey .footer-key--key {
    background: $surface;
    color: $accent;
}

/* ---------------------------------------------------------------- overview: 2 x 2 */

#overview {
    grid-size: 2 2;
    grid-columns: 3fr 2fr;
    grid-rows: 1fr 1fr;
    grid-gutter: 0 2;
    height: 1fr;
    padding: 0 1;
}

#overview > * {
    height: 1fr;
}

/* ---------------------------------------------------------------- agents and session */

#filters {
    height: 1;
    padding: 0 2;
    color: $text-muted;
}

#agents {
    height: 1fr;
}

#session-facts {
    height: auto;
    max-height: 10;
    padding: 0 2;
}

#session-timeline, #session-detail {
    width: 1fr;
}

/* ---------------------------------------------------------------- plugins */

#plugin-side {
    width: 38;
}

#plugins, #plugin-tools, #models {
    height: 1fr;
}

#plugin-calls, #plugin-detail {
    width: 2fr;
}

/* ---------------------------------------------------------------- runs and models */

#models-note {
    padding: 0 2;
    color: $text-muted;
}

/* ---------------------------------------------------------------- full text viewer */

FullText {
    background: $background 80%;
}

#full-text {
    margin: 1 2;
    border: none;
    border-top: solid $accent;
    background: $surface;
}

/* ---------------------------------------------------------------- results */

#files {
    width: 40;
}

#preview-md, #preview-text {
    width: 1fr;
    border: none;
    border-top: solid $panel;
    border-title-color: $text-muted;
    border-title-style: bold;
    padding: 0 1;
}

/* ---------------------------------------------------------------- new mission */

#nm-title {
    height: 1;
    padding: 0 1;
    background: $surface;
}

#nm-help {
    padding: 1 2;
}

#nm-body {
    height: 1fr;
    padding: 1 2;
}

#nm-left, #nm-right {
    width: 1fr;
    padding: 0 1;
}

#nm-body Label {
    color: $text-muted;
    margin-top: 1;
}

#nm-workflow {
    width: 1fr;
}

#nm-request {
    height: 8;
}

#nm-mission {
    height: 2fr;
}

#nm-models {
    height: 1fr;
}

.nm-model-row {
    height: 3;
}

.nm-role {
    width: 14;
    margin-top: 1;
}

.nm-model {
    width: 1fr;
}

.nm-effort {
    width: 16;
}

.nm-buttons {
    height: 3;
    margin-top: 1;
}

#nm-status {
    height: auto;
    margin-top: 1;
}

/* ---------------------------------------------------------------- live tiles */

#live {
    grid-size: 2;
    grid-gutter: 1 2;
    padding: 0 1;
    height: 1fr;
}

.tile {
    height: 1fr;
    border: none;
    border-top: solid $panel;
    border-title-align: left;
    border-title-color: $text;
    padding: 0 1;
    overflow: hidden;
}

.tile:focus {
    border-top: solid $accent;
    background: $surface;
}

/* ---------------------------------------------------------------- dialogs */

Confirm, AskText {
    align: center middle;
    background: $background 60%;
}

.dialog {
    width: 70;
    height: auto;
    padding: 1 2;
    background: $surface;
    border-top: solid $accent;
}

.dialog-buttons {
    height: 3;
    margin-top: 1;
}

.dialog Input {
    margin-top: 1;
}

/* ---------------------------------------------------------------- new mission: options form */

#nm-options {
    height: 1fr;
    background: $surface;
    padding: 0 1;
}

.nm-form-label {
    color: $text-muted;
    text-style: bold;
    margin-top: 1;
}

.nm-check {
    background: $surface;
    border: none;
    padding: 0;
    height: 1;
}

.nm-number-row {
    height: 3;
}

.nm-number-label {
    width: 22;
    margin-top: 1;
}

.nm-number {
    width: 16;
}

.nm-select {
    width: 1fr;
}
